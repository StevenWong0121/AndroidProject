<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.finalyearproject2"
    >
    <uses-feature android:name="android.hardware.CAMERA"/>
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:largeHeap="true"
        android:theme="@style/Theme.FinalYearProject2" >
        <activity
            android:name=".NavigationAdminActivity"
            android:label="@string/title_activity_navigation_admin"
            android:theme="@style/Theme.FinalYearProject2.NoActionBar" >
        </activity>
        <activity
            android:name=".NavigationActivity"
            android:label="@string/title_activity_navigation"
            android:theme="@style/Theme.FinalYearProject2.NoActionBar" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".ChangePasswordActivity"/>
        <activity android:name=".DetailApprovalActivity"/>
        <activity android:name=".DetailHistoryActivity"/>
        <activity android:name=".NewEventActivity"/>
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>